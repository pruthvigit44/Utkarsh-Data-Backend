services:
  - type: web
    name: utkarsh-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "18"
      - key: MONGO_URI
        sync: false
      - key: NODE_ENV
        value: production
